# Copy to .env.local. Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Frontend Sentry — leave blank to disable.
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENV=development
NEXT_PUBLIC_SENTRY_TRACES=0
